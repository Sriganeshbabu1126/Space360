# API Configuration
EXPO_PUBLIC_API_URL=http://localhost:8000

# Note: Firebase configuration is handled natively via google-services.json and GoogleService-Info.plist.
# Place those files in the root of the mobile directory.
